<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, :id=>"title_label" %><br />
    <%= f.text_field :title %>
  </div>
<br />
  <div class="field">
    <%= f.label :starts_at, :id=>"start_label"%><br />
    <%= f.datetime_select :starts_at %>
  </div>
<br />
  <div class="field" id="start_div">
    <%= f.label :ends_at, :id=>"end_label" %><br />
    <%= f.datetime_select :ends_at %>
  </div>
<br />
  <div class="field">
	<%= f.check_box :all_day %>
    <%= f.label :all_day, :id=> "all_day_label" %>
    
  </div>
<br />
  <div class="field">
    <%= f.label :description, :id=>"description_label" %><br />
    <%= f.text_area :description %>
  </div>
<br />
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
